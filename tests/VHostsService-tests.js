describe('VHostsService', function() {
	beforeEach(function() {
		module('app');
	});

	describe('add()', function() {
		it('should append a new vhost to the httpd-vhosts.conf file', function() {
			expect(true).toBe(true);
		});
	});
});
